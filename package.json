{
  "name": "worktree-agent",
  "private": true,
  "scripts": {
    "build": "bun build --compile init.ts --outfile worktree"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.2.0",
    "chalk": "^5.6.2"
  }
}
